server {
    listen 3000;

    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        # This makes React Router work — always serve index.html
        try_files $uri $uri/ /index.html;
    }
}